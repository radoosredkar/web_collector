version: "3.8"
services:
  wscr:
    container_name: wscr
    build: ~/sw/python/web_collector
    environment:
      - FLASK_ENV=development
      - FLASK_APP=main.py
    volumes: 
    - ~/sw/python/web_collector:/usr/src/web_collector
    ports: 
    - "5000:5000"
    #command: /usr/bin/run
